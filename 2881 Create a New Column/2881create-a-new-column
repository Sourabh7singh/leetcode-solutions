import pandas as pd

def createBonusColumn(employees: pd.DataFrame) -> pd.DataFrame:
    employees["bonus"] = 0  
    for i in range(len(employees)):
        employees.loc[i, "bonus"] = employees.loc[i,"salary"]*2
    
    return employees